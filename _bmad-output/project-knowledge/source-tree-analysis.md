# Source Tree Analysis — methodology-rules

_Generated by BMAD document-project (deep scan), 2026-06-23._

## Annotated Tree

```
methodology-rules/
├── apps/
│   └── methodologies/
│       ├── bold/rule-processors/            # Deployable Lambda apps (BOLD MassID, certificate, credit-order)
│       ├── bold-carbon/rule-processors/     # Deployable apps (BOLD Carbon)
│       └── bold-recycling/rule-processors/  # Deployable apps (BOLD Recycling)
│
├── libs/
│   ├── methodologies/
│   │   ├── bold/
│   │   │   └── rule-processors/
│   │   │       ├── mass-id/                  # 20 MassID rule processors (one Nx project each)
│   │   │       │   ├── mass-id-sorting/
│   │   │       │   ├── weighing/
│   │   │       │   ├── geolocation-and-address-precision/
│   │   │       │   ├── prevented-emissions/
│   │   │       │   └── … (16 more)
│   │   │       ├── mass-id-certificate/
│   │   │       │   └── rewards-distribution/
│   │   │       └── credit-order/
│   │   │           └── rewards-distribution/
│   │   ├── bold-carbon/rules/src/            # rules.config.ts — Carbon rule set
│   │   └── bold-recycling/rules/src/         # rules.config.ts — Recycling rule set
│   │
│   └── shared/                              # Processors depend on these ONLY (never cross-processor)
│       ├── app/types/                        # RuleDataProcessor abstract base, app context
│       ├── rule/
│       │   ├── types/                        # RuleInput / RuleOutput / RuleOutputStatus (Zod schemas)
│       │   ├── result/                        # reportRuleResults → SigV4 POST to Smaug
│       │   └── standard-data-processor/       # StandardDataProcessor base behavior
│       ├── lambda/
│       │   ├── wrapper/                       # wrapRuleIntoLambdaHandler (entry-point boilerplate)
│       │   └── types/                         # MethodologyRuleEvent
│       ├── methodologies/bold/                # BOLD-specific shared code:
│       │   ├── processors/                    #   ParentDocumentRuleProcessor + processor bases
│       │   ├── types/                         #   BoldDocumentEventName, document types, enums
│       │   ├── matchers/ utils/ helpers/      #   document-graph matchers & utilities
│       ├── document/                          # DocumentQueryService, document loaders (S3 graph traversal)
│       ├── document-extractor/               # Generic DocumentParser<T> framework
│       ├── document-extractor-scale-ticket/   # Textract OCR parser
│       ├── document-extractor-transport-manifest/
│       ├── document-extractor-recycling-manifest/
│       ├── text-extractor/                    # Text extraction abstractions
│       ├── s3-bucket/                         # S3 operations
│       ├── aws-http/ http-request/            # HTTP + AWS request helpers
│       ├── cloudwatch-metrics/               # CloudWatch metrics publishing
│       ├── env/                               # Environment-variable access
│       ├── helpers/                           # logger (pino), isNil, common utils
│       ├── testing/                           # Shared stubs & test utilities (stubRuleInput, etc.)
│       └── types/                             # Cross-cutting branded types (DocumentId, NonEmptyString)
│
├── tools/
│   ├── create-rule.js                        # Scaffolds a new rule processor (pnpm create-rule)
│   ├── apply-methodology-rule.js             # Wires a rule into a methodology
│   ├── update-waste-classification.js
│   ├── rule-runner-cli/                       # Run a rule locally (pnpm run-rule)
│   ├── document-extractor-cli/               # Extract document data (pnpm extract-document)
│   └── versioning/                           # bump:applications / bump:rules
│
├── .ai/                                       # CANONICAL AI/engineering rules (source of truth)
│   ├── rules/*.md                             # 15 rules (intent / requirements / anti_patterns)
│   ├── STANDARDS.md DEFINITIONS.md PARITY_MATRIX.md PROJECT_CONTEXT.md
│   ├── schemas/ capabilities/
├── .cursor/rules/*.mdc                        # Generated mirror of .ai/rules (Cursor)
├── .claude/skills/rule-*                      # Generated mirror of .ai/rules (Claude)
│
├── .github/workflows/                         # CI: backend-check, check-and-deploy, deploy, generate-readmes, version-and-sync
├── nx.json tsconfig.base.json tsconfig.paths.json
├── eslint.config.js vitest.workspace.ts
└── package.json (.nvmrc → 24.14.1)
```

## Entry Points

- **Per rule:** `{rule}.lambda.ts` → `wrapRuleIntoLambdaHandler(new {Rule}Processor())`. This is the deployable Lambda handler.
- **Local execution:** `tools/rule-runner-cli` (`pnpm run-rule -- <args>`).
- **Scaffolding:** `tools/create-rule.js` (`pnpm create-rule <name> <scope> <description>`).

## Critical Directories

| Directory | Why it matters |
|---|---|
| `libs/methodologies/*/rule-processors/` | All validation logic. One Nx project per rule. |
| `libs/shared/rule/` | The rule contract (input/output) + result reporting to Smaug. |
| `libs/shared/lambda/wrapper/` | The single Lambda boilerplate all rules share. |
| `libs/shared/methodologies/bold/` | Document graph types, matchers, processor base classes. |
| `libs/shared/document/` | S3 document-graph traversal (DocumentQueryService). |
| `libs/shared/testing/` | Reuse stubs here before writing new ones. |
| `tools/` | Scaffolding + codegen; never hand-create processor files. |
| `.ai/` | Source of truth for engineering rules; edit here, then `pnpm ai:sync`. |

## Module-Boundary Rule

Nx tags enforce: `apps → rule-processors → shared`. A processor importing another processor's files, or a relative path crossing an Nx library boundary, is a lint failure. Cross-library imports go through `@carrot-fndn/*` path aliases (`tsconfig.paths.json`).
